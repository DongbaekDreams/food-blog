import React from 'react';
import { Typography } from '@mui/material';

const CountryPage: React.FC = () => (
  <Typography variant="h4">Country Dishes Gallery (Coming Soon)</Typography>
);

export default CountryPage;
