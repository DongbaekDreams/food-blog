import React from 'react';
import { Typography } from '@mui/material';

const RestaurantDetailPage: React.FC = () => (
  <Typography variant="h4">Restaurant Detail (Coming Soon)</Typography>
);

export default RestaurantDetailPage;
