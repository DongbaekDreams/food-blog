import React from 'react';
import { Typography } from '@mui/material';

const DishDetailPage: React.FC = () => (
  <Typography variant="h4">Dish Detail (Coming Soon)</Typography>
);

export default DishDetailPage;
