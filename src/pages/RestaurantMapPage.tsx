import React, { FC } from 'react';
import { Typography } from '@mui/material';

const RestaurantMapPage: FC = () => (
  <Typography variant="h4">Restaurant Visits Map (Coming Soon)</Typography>
);

export default RestaurantMapPage;
