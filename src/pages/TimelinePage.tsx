import React from 'react';
import { Typography } from '@mui/material';

const TimelinePage: React.FC = () => (
  <Typography variant="h4">Timeline (Coming Soon)</Typography>
);

export default TimelinePage;
